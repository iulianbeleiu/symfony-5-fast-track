{% extends 'base.html.twig' %}

{% block title %}Conferences
{% endblock %}

{% block body %}
	{% for conference in conferences %}
		<h4>{{ conference }}</h4>
		<p>
			<a href="{{ path('conference', { id: conference.id }) }}">View</a>
		</p>
	{% endfor %}
{% endblock %}
